pipeline {
  agent any
  stages {
    stage('install dependencies'){
      steps {
        bat 'npm install'
      }
    }
    stage('run test'){
      steps {
        bat 'npm test'
        }
      }
  }
}
